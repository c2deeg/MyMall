<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyMall" parent="Theme.Design.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/userbuttoncolor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/userbuttoncolor</item>
        <item name="colorSecondaryVariant">@color/userbuttoncolor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>



    <style name="AppThemeMaterial" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/shopownerbuttoncolor</item>
        <item name="colorPrimaryDark">@color/shopownerbuttoncolor</item>
        <item name="colorAccent">@color/shopownerbuttoncolor</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">15dp</item>
        <item name="colorControlNormal">@color/silver</item>
        <item name="colorControlActivated">@color/orange</item>
    </style>

    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/star</item>
        <item name="android:minHeight">22dip</item>
        <item name="android:maxHeight">22dip</item>
        <item name="colorControlNormal">@color/silver</item>
        <item name="colorControlActivated">@color/orange</item>
    </style>

</resources>